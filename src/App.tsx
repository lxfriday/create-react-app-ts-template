import React from 'react'
import { hot } from 'react-hot-loader/root'
import './App.css'

const App: React.FC = () => {
  return (
    <div className="App">
      <header className="App-header">
        <p>
          are i know ok <code>src/App.tsx</code> and save to reload.
        </p>
      </header>
    </div>
  )
}

export default hot(App)
